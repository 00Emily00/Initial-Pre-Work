// by doing an === you are already telling it whether it will be true or false. So instead of doing an if statement and then returning true, you can just return a === b;

function isLess(a, b) {
  // Fix this code

    return a < b;
}

// Change these values to test
isLess(10, 15);