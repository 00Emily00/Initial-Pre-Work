// A simple way of writing an if else statement is by using the ternary operator (conditional operator). syntax is: condition ? statement-if-strue : statement-if-false;

function checkEqual(a, b) {
  return a == b ? true : false;
}

checkEqual(1, 2);