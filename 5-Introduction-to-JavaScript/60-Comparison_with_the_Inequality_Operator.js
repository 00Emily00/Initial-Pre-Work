// Just like equality operator, there is an inequality operator (!=). This mean that something is not the same as something else (it is false)

// Setup
function testNotEqual(val) {
  if (val != 99) { // Change this line
    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testNotEqual(10);