// You can generate a random fraction using the Math.random() built in function. Math.random() can never really get to 1 because it is always rounding down

function randomFraction() {

  // Only change code below this line.

  return Math.random();


  // Only change code above this line.
}