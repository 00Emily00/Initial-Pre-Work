// You can also count backwards with the for loop by changing the initialization to start at the highest number, switch the signs in the condition, and decrement instead of increment in the final expression

// Example
var ourArray = [];

for (var i = 10; i > 0; i -= 2) {
  ourArray.push(i);
}

// Setup
var myArray = [];

// Only change code below this line.
for (var i = 9; i > 0; i-=2) {
  myArray.push(i);

}