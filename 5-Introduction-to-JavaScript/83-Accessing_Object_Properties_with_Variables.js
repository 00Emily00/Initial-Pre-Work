// You can also use bracket notations on objects to access a property which is stored as the value of a variable. Useful when iterating (looking) through an object's properties

// Setup
var testObj = {
  12: "Namath",
  16: "Montana",
  19: "Unitas"
};

// Only change code below this line;

var playerNumber = 16;       // Change this Line
var player = testObj[playerNumber];   // Change this Line